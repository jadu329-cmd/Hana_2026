rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // appData 컬렉션에 대한 읽기/쓰기 허용 (모든 사용자)
    match /appData/{document=**} {
      allow read, write: if true;
    }
  }
}
